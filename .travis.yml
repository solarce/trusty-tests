sudo: required
language: ruby
services:
  - docker
script:
  - lsb_release -a
  - docker --version
  - docker pull alpine:3.2
  - docker run -d -i -t --name foo alpine:3.2 ash
  - docker ps
  - docker exec foo echo "hello travis"
